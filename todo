
more effects ( low prio)
add vocoder ( verry low prio)


project sorage and export,
render and export song feature

multi user featuer so more than one can work on the same project 

loop sequencer -> internal synth option           [DONE] SEQ Out selector: MIDI port or PRIZM; preview/PREVIEW/looped playback/bounce route to PRIZM via new playScheduled (sample-accurate scheduled voices)
