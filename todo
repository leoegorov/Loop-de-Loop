808 style drums                              [DONE] built-in synthesized 808 kit (kick/snare/hats/clap) + 16-step sequencer, DRUMS button / D key
vst support                                  [NOT FEASIBLE in browser] no plugin hosting in Web Audio; options: Web Audio Modules (limited catalog) or native JUCE port
midi pattern generation                      [DONE] SEQ button per channel: piano-roll sequencer -> MIDI out; REC LOOP bounces the pattern into the loop as audio
beat synced record end possibility per loop  [DONE] per-loop quantize override (Q dropdown in each channel header: glob/bar/beat/off)

import function for audio and midi, compatible with allready exported files from ealrier versions
                                             [DONE] IMPORT button: reads exported zips (wav+mid+manifest, tempo restored), loose .wav/.mid, deflate zips; loops land stopped, bar-aligned on first play
play all plays everything in synced          [DONE] PLAY ALL starts loops + 808 + 303 on one shared bar downbeat (grid loops rejoin in phase, free loops restart at 0)
works on github pages without the server on port 8080   [FIXED - push + enable "Enforce HTTPS" in repo Settings->Pages] http visitors lacked mic/MIDI/AudioWorklet and errors were invisible; now auto-redirects to https and shows startup errors on the power screen

options for custom drum sound import, and spawning more chanels as well as the option to create polyrythms
                                             [DONE] +SAMPLE imports audio files as drum rows, +VOICE spawns extra synth rows (removable), per-row step count 2-32 creates polyrhythms with per-row playheads

add automation to 303

add more synth engines
add vocoder 
transpose feature                            [DONE] per-loop Pitch field (+/-24 st): granular pitch shift at playback, tempo/grid locked, MIDI notes follow, non-destructive
song build from loops feature
more effects
effect automation
